import SwiftUI

struct ContentView: View {
    var body: some View {
        VStack {
            Text("HEIMDALL Control Surface")
                .font(.largeTitle)
            Text("v0.1.0")
                .foregroundStyle(.secondary)
        }
        .padding()
    }
}

#Preview {
    ContentView()
}
